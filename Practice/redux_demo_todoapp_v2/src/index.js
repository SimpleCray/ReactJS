import React from 'react';
import ReactDOM from 'react-dom';
import TodoApp from './smartComponents/TodoApp';

ReactDOM.render(
  // <Provider store={store}>
    <TodoApp/>,
  // </Provider>,
  document.getElementById('root')
);

